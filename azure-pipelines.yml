# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
# - task: SonarCloudPrepare@1
#   inputs:
#     SonarCloud: 'SonarCloud-Frontend'
#     organization: 'jannotjarks'
#     scannerMode: 'CLI'
#     configMode: 'manual'
#     cliProjectKey: 'jannotjarks_E-Paper_Frontend'
#     cliProjectName: 'jannotjarks_E-Paper_Frontend'
#     cliSources: '.'
#     extraProperties: |
#       # Additional properties that will be passed to the scanner, 
#       # Put one key=value per line, example:
#       # sonar.exclusions=**/*.bin
#       sonar.sources=src
#       sonar.exclusions=**/node_modules/**
#       sonar.tests=src
#       sonar.test.inclusions=**/*.spec.ts
#       sonar.typescript.lcov.reportPaths=coverage/lcov.info
    
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    ng build
  displayName: 'npm install and build'
  
# - task: SonarCloudPublish@1
#   inputs:
#     pollingTimeoutSec: '300'

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: $(System.DefaultWorkingDirectory)/dist/e-paper-frontend/
    artifactName: app